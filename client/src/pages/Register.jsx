import { ArrowLeft, Stethoscope } from 'lucide-react';
import { useState } from 'react';
import { Link, useNavigate } from 'react-router-dom';
import api from '../lib/api';

export default function Register(){const [form,setForm]=useState({fullName:'',email:'',phone:'',password:''});const [msg,setMsg]=useState('');const [ok,setOk]=useState(false);const nav=useNavigate();const submit=async(e)=>{e.preventDefault();try{const {data}=await api.post('/auth/register',form);setOk(true);setMsg(data.message);setTimeout(()=>nav('/login'),1000)}catch(err){setOk(false);setMsg(err.response?.data?.message||'Registration failed.')}};return <div className="soft-grid flex min-h-screen items-center justify-center bg-slate-100 px-5 py-12"><form onSubmit={submit} className="app-card w-full max-w-2xl rounded-3xl p-7 sm:p-10"><div className="flex items-center justify-between"><Link to="/" className="inline-flex items-center gap-2 text-sm font-bold text-slate-500 hover:text-indigo-600"><ArrowLeft size={16}/>Back home</Link><span className="brand-mark"><Stethoscope size={20}/></span></div><p className="eyebrow mt-9">Patient registration</p><h1 className="page-title mt-2 text-4xl font-extrabold">Create your account</h1><p className="mt-3 text-slate-500">Start booking appointments and tracking live queues in minutes.</p>{msg&&<div className={`mt-5 rounded-xl border p-3.5 text-sm font-medium ${ok?'border-green-100 bg-green-50 text-green-700':'border-red-100 bg-red-50 text-red-700'}`}>{msg}</div>}<div className="mt-7 grid gap-5 sm:grid-cols-2">{[['fullName','Full name','text'],['email','Email address','email'],['phone','Phone number','tel'],['password','Password','password']].map(([n,l,t])=><label key={n} className="text-sm font-bold text-slate-700">{l}<input required minLength={n==='password'?6:undefined} type={t} value={form[n]} onChange={e=>setForm({...form,[n]:e.target.value})} className="mt-2 w-full rounded-xl border px-4 py-3" placeholder={n==='password'?'At least 6 characters':''}/></label>)}</div><button className="mt-8 w-full rounded-xl bg-indigo-600 py-3.5 font-bold text-white shadow-lg shadow-indigo-200 hover:bg-indigo-700">Create patient account</button><p className="mt-6 text-center text-sm text-slate-500">Already registered? <Link to="/login" className="font-bold text-indigo-600">Log in</Link></p></form></div>}
