import { Calendar, MapPin, Stethoscope } from 'lucide-react';import { useEffect,useState } from 'react';import { Link } from 'react-router-dom';import PageHeader from '../components/PageHeader';import api from '../lib/api';
export default function Doctors(){const [doctors,setDoctors]=useState([]);const [loading,setLoading]=useState(true);useEffect(()=>{api.get('/users/doctors').then(r=>setDoctors(r.data.doctors)).finally(()=>setLoading(false))},[]);return <div className="min-h-screen bg-slate-100"><PageHeader title="Find Doctors" backTo="/dashboard"/><main className="mx-auto max-w-7xl px-6 py-10">{loading?<p className="mt-8">Loading...</p>:<div className="mt-8 grid gap-6 md:grid-cols-2 lg:grid-cols-3">{doctors.map(d=><article key={d.id} className="rounded-3xl bg-white p-6 shadow"><div className="flex h-16 w-16 items-center justify-center rounded-2xl bg-blue-100 text-blue-600"><Stethoscope size={30}/></div><h2 className="mt-5 text-xl font-bold">{d.name}</h2><p className="font-semibold text-blue-600">{d.specialty||'General Medicine'}</p><p className="mt-2 text-sm text-slate-500">{d.experience||'Experience not specified'}</p><p className="mt-4 flex gap-2 text-sm text-slate-600"><MapPin size={17}/>{d.location||'DocFlow Clinic'}</p><div className="mt-6 flex items-end justify-between"><div><p className="text-sm text-slate-500">Fee</p><b className="text-xl">৳{d.fee||0}</b></div><Link to={`/book-appointment/${d.id}`} className="flex items-center gap-2 rounded-xl bg-blue-600 px-4 py-3 font-semibold text-white"><Calendar size={18}/>Book</Link></div></article>)}</div>}</main></div>}
